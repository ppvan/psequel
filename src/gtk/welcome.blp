using Gtk 4.0;
using Gio 2.0;
using Adw 1;

template $SequelizeConnectionView : Adw.Bin {
    hexpand: true;
    vexpand: true;

    Gtk.Paned paned {
        // can-shrink: false;

        [start]
        $SequelizeConnectionSidebar sidebar {
            width-request: 320;
        }

        [end]
        $SequelizeConnectionForm form {

        }
    }

}