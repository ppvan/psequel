namespace Psequel {

    /**
     * Carying schema info binded to widgets to update if needed.
     */
    public class Schema : Object {
        public ObservableArrayList<string> tablenames {get; set;}
    }
}