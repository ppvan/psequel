using Gtk 4.0;
using Adw 1;

template $PsequelTableColInfo : Adw.Bin {
    ScrolledWindow {
        ColumnView view {

            styles ["data-table"]
            show-column-separators: true;
            show-row-separators: true;
            vexpand: true;
        }
    }
}